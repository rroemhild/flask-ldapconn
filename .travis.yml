sudo: required

language: python

services:
  - docker

before_install:
- docker pull rroemhild/test-openldap

python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"

install:
  - pip install pipenv
  - pipenv install --dev

script: python test_flask_ldapconn.py
